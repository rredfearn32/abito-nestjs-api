{
  "version": 2,
  "buildCommand": "npx prisma migrate deploy && npm run vercel-build",
  "crons": [
    {
      "path": "/cron/expire-streaks",
      "schedule": "0 23 * * *"
    }
  ],
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/swagger-static/(.*)",
      "dest": "/dist/swagger-static/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/api/index.js"
    }
  ]
}
